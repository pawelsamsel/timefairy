# Timefairy Desktop (scaffold)

Placeholder for Tauri 2 + React macOS app. Share HTTP logic with CLI via `crates/core`.

```bash
npm create tauri-app@latest
```

Requires Xcode Command Line Tools on macOS. Devbox provides Rust toolchain.

Connect to API: `TIMEFAIRY_API_URL=http://localhost:3000`
