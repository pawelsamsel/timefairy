# Timefairy Mobile (scaffold)

Placeholder for React Native Expo app. Connect to API via `@timefairy/api-client`.

```bash
npx create-expo-app@latest . --template tabs
pnpm add @timefairy/api-client @timefairy/shared-types
```

Set `EXPO_PUBLIC_API_URL=http://localhost:3000` for local dev.
